// Vite entrypoint.
//
// The current quiz runtime still loads as classic browser scripts because it
// relies on shared window globals. This file gives the project a modern Vite
// entrypoint now, and it is the future place to import modularized app code as
// src/app.js is split apart.

